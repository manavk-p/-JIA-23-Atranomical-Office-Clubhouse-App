import { StatusBar } from "expo-status-bar";
import React, { useState } from "react";

import {
  Text,
  SafeAreaView,
} from "react-native";

const Home = ({}) => {
  return (
    <SafeAreaView>
      <Text>{"Hello World"}</Text>
    </SafeAreaView>
  );
}

export default Home;
